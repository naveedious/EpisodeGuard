# ── Required: API credentials (never stored in DB) ───────────────────────
TAUTULLI_URL=http://tautulli:8181
TAUTULLI_API_KEY=your_tautulli_api_key

SONARR_URL=http://sonarr:8989
SONARR_API_KEY=your_sonarr_api_key

# ── Web UI basic auth ─────────────────────────────────────────────────────
WEB_USERNAME=admin
WEB_PASSWORD=changeme

# ── Optional ──────────────────────────────────────────────────────────────
# Path inside container for SQLite DB. Default: /data
DATA_DIR=/data

# Note: poll interval, lookahead, season end buffer, log retention, webhook mode,
# and Apprise notifications are all configurable via the Settings page in the UI.
# Defaults are applied on first run.

# ── Webhook (optional) ────────────────────────────────────────────────────
# When webhook mode is enabled in settings, configure Tautulli to POST to:
#   http://<episodeguard-host>:8988/api/webhook
# Playback Start agent → Webhook → Method: POST, Content-Type: application/json
# JSON body must include: media_type, grandparent_title, parent_media_index,
#   media_index, grandparent_guids
